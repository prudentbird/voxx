---
"@prudentbird/voxx": patch
---

Sync scaffolded `robots.ts` and `sitemap.ts` templates with the dogfooded web app — newly initialized projects now get the `disallow` rules and sitemap `changeFrequency`/`priority` SEO improvements.
